# 开发规范（AI Agent 优先）

> 本文档面向**任何打算在本仓库上开发功能的 AI Agent**（及其背后的使用者）。
> 本项目是 **AI-first**：程序只面向 AI Agent 使用与扩展，默认不考虑人类直接操作或编码。
> 阅读顺序建议：README（项目概览）→ AGENTS.md（环境配置）→ 本文档（开发约束）。
> 动手开发前请完整阅读本文档，**第 3 节「跨平台约束」与第 6 节「高性能约束」是必读**。

## 1. 总体原则：AI 完成开发，人类提出需求

- **本项目是 AI Agent 优先（AI-first）**：配置、使用、扩展、维护全流程都通过 AI Agent 完成。程序面向 Agent 开发，不要求人类直接编码。
- **开发流程**：人类**不直接编码**。添加 / 修改功能的标准流程是：使用者向 AI Agent 提出功能需求 → Agent 阅读本文档与相关代码 → **Agent 优先检索库内可复用资源（已有模块 / 共享类 / 资源 / 封装，见 §5「复用优先」）** → Agent 实现 → Agent 自测验证 → 使用者验收 →（可选）Agent 提交 PR 惠及上游。
- **fork 自由，自用随意**：本仓库采用 MIT 协议，任何人可以 fork，让 AI Agent 按自己的需求添加 / 修改功能，无需征得作者同意。自用 fork 想怎么改都行。
- **PR 是自愿的**：自用功能不必提 PR。只有当你想让改动惠及上游（合回主仓库、让所有人受益）时，才需要走 PR 流程。**例外：缺陷上报（下一条）是义务，不因 fork 自用而豁免。**
- **发现缺陷必须主动上报**：Agent 在配置、使用、维护本软件过程中，若发现原始代码存在漏洞 / bug（功能异常、数据损坏风险、安全隐患等），**应当主动提交 issue 描述问题（现象 + 复现步骤 + 影响范围），并提交修复 PR**，让所有使用者受益——而不是只在本地 fork 静默修掉、忽略或跳过。上报前先搜索 issue / PR 是否已存在，避免重复；若已存在且无人处理，可以补充复现信息或认领修复。修复 PR 同样遵守 §2 全部要求；若暂无法自行修复，至少完成根因定位（到场景 / 脚本 / 函数级别）再提交 issue，由作者或其他 Agent 跟进处理。
- **先讨论再大改**：涉及架构级改动（场景树结构变更、核心脚本重构、渲染管线 / 资源管线变更）时，建议先开 issue 说明方案，减少返工。
- **对 Agent 的要求**：Agent 是功能实现的执行者，必须遵守本文档全部约束；遇到超出能力范围的决策（如破坏性接口变更、隐私边界问题），应明确告知使用者而不是擅自决定。
- **人工操作边界**：**Godot 的安装**与**项目骨架（`project.godot` / `scenes/` / `scripts/` 等）的创建**由使用者（人类）**手动完成**——Agent 只提供操作指引与就绪检查（如 `godot --version`），**不代为安装 Godot、不代为创建骨架**。骨架就绪后的配置、开发、运行由 Agent 完成。
- **创建项目必须询问存放路径**：使用者要求 Agent 创建 / 初始化 Godot 项目时，Agent **必须先询问项目存放路径**（本地绝对路径），确认后再执行职责内操作；**禁止**在未确认路径时擅自假定或猜测路径创建项目。指引中的示例路径仅为示意，实际操作以使用者确认的路径为准（详见 AGENTS.md §1.3）。
- **Godot MCP 检测（可选）**：Agent 涉及编辑器交互开发前，应检测工程是否安装 godot-mcp（如 `addons/` 下存在 godot-mcp 插件且已启用）；未安装时**明确告知使用者并请求安装**，经使用者确认后 **Agent 可介入代替用户处理安装**（下载插件到 `addons/`、启用插件、配置 MCP server 等，须征得同意、遵循工程独立性约定、安装后报告并复检）；使用者不同意或安装失败时 Agent 不得阻塞开发，降级为命令行方式（`godot --headless --path .` 等）继续（详见 AGENTS.md §1.4）。
- **美术与光照例外（人类掌控）**：光照、材质、色调、美术资源、音频（音效 / 音乐 / 混音）等创作与视觉听觉表现相关的内容，控制权归使用者（人类）。Agent **不得擅自创建、修改或"优化"光照、美术、材质与音频内容**；Agent 可以提供技术方案与性能建议（如渲染优化实现、资源格式建议、音频流式加载），但创意与听觉视觉决策必须由人类主导拍板。涉及上述领域的改动必须在 PR 描述中注明已经人类确认。
- **身份表达（以 AI Agent 口吻提交）**：Agent 在 issue / PR / commit / 评论中的所有文字，一律以**自身（AI Agent）的口吻**书写，不以背后使用者（人类）的口吻或人称发言。示例：用"我的使用者提出…"或直接陈述需求/结论，不用"我要…我做了…"这类人类第一人称；提交者署名（commit author）保持为 Agent 自己的身份，不得冒用使用者的名字或账号。让作者和审查方（也可能都是 AI Agent）能一眼区分"这是 Agent 提交的内容"与"这是使用者本人的需求"。
- **Agent 工具链与 Godot 工程独立性（人类随时可人工接管）**：Agent 的工具链（自动化脚本 / 工具 / 专属文件）与 Godot 工程文件（`project.godot` / `scenes/` / `scripts/` / `modules/` / `glue/` / `autoload/` / `addons/` / `tests/`）**物理隔离、互不依赖**。Agent 专属内容一律放仓库根级独立目录 `agent/`，禁止混入工程目录；工程禁止引用 `res://agent/` 下任何内容；删除 / 停用 `agent/` 后工程必须仍可正常打开、加载、运行。工程始终保持「脱离 Agent 工具链亦可人工操作」的自洽状态，人类可随时在 Godot 编辑器中接管开发，Agent 不持有独占锁、不留「必须由 Agent 维护」的文件，交接时在 `agent/` 记录进度与未提交内容（详见 AGENTS.md §4.5）。

## 2. 提交 PR 的要求

PR 由 AI Agent 编写提交（人类只提出需求、不直接编码）。以下要求不满足的 PR 不会被合并：

1. **单一职责**：一个 PR 只做一件事（一个 feature 或一个 fix）。夹带无关重构、格式化、改名会整单打回。
2. **不引入个人环境的硬编码**：禁止本机路径（`C:\Users\xxx`、`/home/xxx`）、个人设备信息、个人账号信息。详见第 3 节。
3. **不破坏现有行为**：现有场景 / 脚本 / 信号的调用方式与接口不得随意改变；已有功能不能回归；Autoload 单例与公共 API 保持兼容。
4. **资源与场景变更需说明**：新增 / 修改 `.tscn` / `.tres` / 资源文件时，在 PR 描述中说明变更内容与影响范围；二进制资源（纹理 / 音频 / 模型）尽量用 Godot 可导入格式提交，避免提交 `.godot/` 导入缓存。
5. **新增功能需同步文档**：在 README「功能」章节登记；若影响 AGENTS.md 或 `docs/` 下的文档，也要同步更新。
6. **提交信息遵循 Conventional Commits**：`feat:` / `fix:` / `docs:` / `refactor:` / `chore:` 前缀（参考仓库 git log 风格）。
7. **版本号与发布由作者决定**：贡献者不要修改 `project.godot` 中的 `config/version`、不要打 tag、不要创建 release。
8. **文档使用中文**：README / AGENTS.md / docs 均为中文，新增或更新的文档用中文写。
9. **不提交任何密钥**：API key、token、密码、签名密钥严禁出现在 commit、PR 描述、issue、日志或测试输出中。提交前自查。
10. **MIT 许可延续**：保持 MIT 许可证及版权声明；提交代码即视为同意以 MIT 协议授权给项目。
11. **验证说明**：PR 描述必须写明「需求来源 → 实现方式 → 验证过程与结果」三段式说明（PR 的审查方也可能是 AI Agent，需要可复现的验证信息）。Godot 项目请说明验证用的 Godot 版本、运行方式（`godot --headless --path .` 跑测试 / 编辑器内手动验证 / 导出后验证）。
12. **平台专属代码（如 Windows 专属 API、Android 权限）必须满足**：平台门控（非目标平台直接禁用或优雅降级）、探测失败静默回退到跨平台路径、封装为独立模块共用同一入口、文档中标注适用平台与回退行为。禁止以「平台专属」为由绕过第 2 条（无个人环境硬编码）与第 5 条（文档同步）。
13. **以 AI Agent 口吻提交**：commit 信息、PR 描述、issue 正文一律用 Agent 自身口吻书写（见 §1「身份表达」），不冒用背后使用者的人称；需求来源段落（第 11 条三段式说明中的"需求来源"）写"使用者提出…"而非"我需要…"，让作者与审查方清楚区分 Agent 的陈述与使用者的原始需求。
14. **不提交任何美术资源（Asset）**：贴图、音频、模型、字体等美术资源（png/jpg/webp/ogg/wav/glb/fbx/ttf 等格式及 `assets/`、`modules/*/assets/` 目录）**严禁出现在 commit / PR 中**——仓库只保留功能实现代码与封装。新增资源需求在 `docs/RESOURCES.md` 登记清单，由使用者本地提供。唯一例外：`icon.svg`。

> 目前仓库没有 CI，上述脚本是手动验证工具。合并决策由作者（或其 AI Agent）实际运行验证后作出。

## 3. 跨平台约束（重点，必读）

**本项目目标平台为：Windows 桌面（Windows 10 / 11）与 Android 移动端。** 开发时必须遵守以下约束：

> **关于 Windows 7**：经评估放弃支持。Godot 官方自 4.5 起将最低支持版本提升至 Windows 10（[PR #106959](https://github.com/godotengine/godot/pull/106959)），4.5 dev 起 Windows 7 兼容已破坏；且 Windows 7 已停止安全更新多年。本项目锁定 Godot 4.5+，最低 Windows 版本为 **Windows 10**。

### 3.1 不假设操作系统与桌面环境

- 路径拼接必须用 `ProjectSettings` / `OS.get_user_data_dir()` / `DirAccess` / `FileAccess` API，禁止手写 `/` 或 `\` 分隔符。
- 禁止 spawn 依赖平台的外壳命令（cmd / PowerShell / bash 专属命令）；确需外部进程时，说明跨平台方案。
- 注意文件路径大小写敏感（Linux/Android）与不敏感（Windows 默认）的差异、权限模型差异。
- Windows 与 Android 的能力差异必须显式处理：仅桌面存在的能力（注册表、Win32 API）在 Android 上必须禁用或降级；仅移动端存在的能力（传感器、触摸手势）在 Windows 上必须禁用或降级。

### 3.2 Windows 桌面专项约束

- **最低支持 Windows 10**（x86_64；arm64 可选）；开发验证环境以 Windows 10/11 为准。
- 渲染驱动：Godot 4.x 默认 Vulkan（Forward+ / Mobile）；Windows 7 时代遗留的显卡驱动可能不支持 Vulkan，本项目不为此做兼容——目标硬件需支持 Vulkan 1.0+ 或 Direct3D 12。
- 使用 Win32 API / 命名管道等 Windows 专属能力时，必须：`OS.has_feature("windows")` 门控、失败优雅降级、封装为独立模块、文档标注（见 §2 第 12 条）。
- 窗口 / 显示相关代码不假设特定 DPI 或分辨率，用 `DisplayServer` API。

### 3.3 Android 移动端专项约束

- **最低 Android 版本**：跟随 Godot 4.5 的官方要求（Android 6.0 / API 23 起；实际验证以 Android 8+ / API 26 为主流基线）。
- 移动端性能与内存受限：纹理压缩（ASTC/ETC2）、音频流式加载、避免常驻大资源。
- Android 权限（网络、存储等）需在导出预设中声明；运行时权限用 Godot 权限 API 请求。
- 触摸输入 / 传感器 / 多指手势按 Godot 输入映射处理，不写死平台事件。
- 涉及平台差异的代码用 `OS.has_feature("android")` 等特性检测门控，禁止写死平台名。

### 3.4 原生扩展要谨慎

- 尽量使用纯 GDScript / 内置节点实现功能；确需 GDExtension / C# 时，必须在 PR 中说明对 Windows 与 Android 两个目标平台的覆盖情况与备选方案。
- 不要假设目标机器有编译工具链；GDExtension 需提供 Windows 与 Android 的预编译产物（含 ABI 匹配说明）或清晰的构建说明。

### 3.5 运行参数与配置

- 路径、端口、开关等运行参数应通过 `ProjectSettings`（`project.godot` 自定义项）或环境变量 / 命令行参数（`OS.get_cmdline_user_args()`）配置，禁止新增硬编码。
- 敏感配置（如 API key）走环境变量或 `user://` 下的本地配置，**禁止**写进 `project.godot` 提交到仓库。

### 3.6 网络环境差异

- 若项目涉及网络功能：HTTP 请求必须带超时与错误处理；弱网 / 断线要有重试或明确失败提示；不得因网络异常阻塞主线程（用 `HTTPRequest` 异步节点或协程）。
- Android 网络环境波动大（Wi-Fi / 移动数据切换、后台挂起），网络逻辑必须能优雅恢复。
- 禁止在代码中硬编码个人代理地址 / 个人服务器地址。

### 3.7 时区与本地化

- 时间戳的存储与展示要明确时区语义：推荐存储 UTC（`Time.get_datetime_string_from_system(false)` 或自管 epoch），展示层再转本地时区。
- 禁止假设运行机器与「看数据的人」在同一个时区。
- 面向多语言时，文案走 `tr()` 与翻译表（`.csv` / PO），禁止散落硬编码字符串。

### 3.8 资源与数据可迁移

- 项目资源（场景 / 脚本 / 素材）应可整体拷贝；禁止在代码中写死绝对路径，一律相对项目根或用 `user://`。
- 存档 / 用户数据走 `user://`（各平台自动映射到合法位置，Windows 为 `%APPDATA%`、Android 为应用私有目录），不写死在项目目录。

### 3.9 无头 / CI 场景

- CI / 自动化验证用 `godot --headless` 运行；核心逻辑与表现分离，纯逻辑测试不依赖渲染。
- 日志走 stdout（`print` / `push_error` 即可被 CI 采集），不要新增「写日志文件」的逻辑（或做成可选）。
- 信号处理：进程要能优雅处理 `SIGTERM` / `SIGINT`（`SceneTree.quit()` 前收尾），让容器编排能安全停止。

## 4. 数据与隐私边界

- 本工具只处理**使用者授权范围内**的数据；涉及用户数据采集 / 上传的功能必须明示并征得使用者同意。
- 禁止批量抓取第三方数据、规避限流做数据挖掘、或用于骚扰、人肉等用途。
- 采集的数据仅用于既定用途，代码不得把数据上传到任何未声明的第三方服务。
- 不得在公开代码 / 文档 / 示例中夹带任何真实用户（作者本人除外）的隐私信息。

## 5. 代码规范（GDScript）

- **语言**：GDScript（Godot 4.x 语法）；确需 C# / GDExtension 的场景按 §3.4 走。
- **Godot 版本**：≥ 4.5（本项目锁定 4.5+，见 §3）。`project.godot` 中 `config/features` 声明所用特性集，升级大版本需单独 PR 并全量回归。
- **风格**：遵循官方 [GDScript 风格指南](https://docs.godotengine.org/en/stable/tutorials/scripting/gdscript/gdscript_styleguide.html)：
  - 命名：`snake_case` 变量 / 函数，`PascalCase` 类名 / 节点名（`@export` 变量可 `PascalCase` 便于编辑器显示）；
  - 缩进：Tab；行宽 ≤ 100；
  - 常量 `UPPER_SNAKE_CASE`；私有成员前缀 `_`；
  - 用 `@onready` 延迟获取节点，避免 `get_node()` 散落。
- **静态类型**：所有变量 / 参数 / 返回值声明类型（`var x: int`、`func f(a: Node) -> void`），提升性能与可读性。
- **信号优先**：节点间通信用信号（`signal` + `emit`），避免直接引用耦合；跨场景通信用 Autoload 单例或信号总线。
- **模块化与 glue 架构（项目核心架构原则）**：功能必须**模块化、可复用**,模块之间**只通过 glue（胶水）代码链接**,禁止模块间直接耦合。具体约束：
  - **复用优先（实现功能前必查）**：开发任何新功能前，Agent **必须优先检索仓库内已有的模块 / 共享类（`scripts/`）/ 资源 / 封装**，确认是否存在可复用的实现——存在则直接复用（经 glue 接线），**禁止重复造轮子**；只有在确认无可复用资源时才新建模块。复用检索的检查项：`modules/` 下已有模块、`scripts/` 共享层、`docs/RESOURCES.md` 资源清单、`docs/` 功能记录。
  - **模块自包含**：每个功能模块是独立单元（模块目录内自带场景 / 脚本 / 资源 / 数据），不依赖其他模块的内部实现；模块可整体复制到其他项目复用。
  - **模块只暴露公开接口**：模块通过 `class_name` 公共类、信号、公开方法对外提供能力；模块内部实现（私有成员 `_`、内部节点、内部场景）对其他模块不可见、不可引用。
  - **glue 代码负责接线**：模块之间的组装、消息路由、生命周期编排一律由 glue 层完成——glue 只做「连接」不做「业务」。典型 glue 形态：Autoload 信号总线（模块发信号、glue 转发）、服务注册表（模块注册、glue 分发）、主场景组装脚本（实例化模块节点并接线）。
  - **禁止模块间直接引用**：模块 A 不得直接实例化 / 调用模块 B 的内部类、不得直接引用模块 B 的场景与资源、不得 `get_node` 到其他模块内部。需要协作时：A 发信号 → glue 转发 → B 响应；或 A 经 glue 获取 B 的公开接口。
  - **glue 保持薄**：glue 代码不包含业务逻辑、不承载状态（除注册表数据）、不重复实现模块能力；业务逻辑一律在模块内部。
  - **新增功能的默认形态**：先评估能否做成独立可复用模块（自带场景 + 逻辑 + 数据）；若需与其他功能协作，通过 glue 接线，禁止在模块内写死对其他模块的依赖。
- **模块划分**（在模块化架构下的目录约定）：
  - 每个功能模块一个目录（`modules/<module_name>/`，内含该模块的场景 / 脚本 / 资源 / 数据），模块内部可再按 `scenes/` `scripts/` `assets/` 细分；
  - 跨模块共享的纯逻辑类（`class_name`，不依赖场景树）放 `scripts/` 共享层，供各模块复用（共享层不属于任何模块，属于 glue/基础设施）；
  - 全局状态 / 服务用 Autoload 单例（`autoload/`）——Autoload 属于 glue 层，负责模块间消息总线与服务注册，禁止在 Autoload 中堆积业务逻辑；禁止全局变量散落。
- **资源管理（Asset 不入库）**：**GitHub 仓库只提交功能实现代码与封装，不包含任何贴图、音频、模型、字体等美术资源**（由使用者本地提供，见 §2 第 14 条与 README「资源管理」）：
  - 美术资源放 `assets/`（全局）或 `modules/<模块名>/assets/`（模块私有），两者均已被 .gitignore 排除；
  - 场景引用资源一律用 `@export` 暴露路径，禁止写死资源绝对路径；资源缺失时用占位（`PlaceholderTexture2D` / 代码生成占位纹理 / 程序化生成）保证项目可加载、测试可运行；
  - 新增资源需求必须在 `docs/RESOURCES.md` 登记（路径、用途、建议规格），供使用者按清单放置；
  - 唯一例外：`icon.svg`（Godot 项目必需图标）随仓库提交。
- **Agent 工具链隔离（工程独立性）**：Agent 的自动化脚本 / 工具 / 专属文件一律放仓库根级独立目录 `agent/`，**禁止**混入 `scenes/` / `scripts/` / `modules/` / `glue/` / `autoload/` / `addons/` / `tests/` 等工程目录；工程代码禁止 `load()` / `preload()` / 引用 `res://agent/` 下任何资源。工程保持「无 Agent 工具链亦可加载运行」的自洽状态，人类可随时在 Godot 中人工接管（详见 AGENTS.md §4.5）。
- **错误处理**：异步路径必须有 try/catch 或 await 错误处理；信号回调不得因单个异常导致整个场景崩溃（Godot 4 默认异常会中断调用链，必要时用 `push_error` 记录后降级）。
- **日志**：`print` / `push_warning` / `push_error`，中文描述，不引入额外日志库。
- **性能红线**（详见 §6）：热点路径禁止每帧 `new` 对象、禁止每帧字符串拼接、禁止在 `_process` 中做 IO / 网络 / 资源加载。

## 6. 高性能约束（重点，必读）

**以 60 FPS 为基准**，低配设备（核显 / Android 移动端）也应流畅。遵守以下约定：

- **静态类型优先**：未类型化的 GDScript 在解释路径显著变慢；热路径必须全类型化。
- **避免每帧分配**：`_process` / `_physics_process` 内禁止创建对象（`Vector2()` 字面量除外）、数组、字典；需要复用用成员变量或对象池。
- **禁止每帧 IO / 网络 / 资源加载**：这些操作放协程（`await`）、线程或事件驱动；`FileAccess` / `HTTPRequest` / `load()` 绝不进 `_process`。
- **渲染优化**：
  - 控制 draw call：静态物体合并网格（`MultiMesh` / `CSG` 合并）、少用半透明重叠；
  - 光照：优先烘焙光照贴图，动态光数量克制；阴影用低分辨率或禁用远处阴影；**光照方案属美术决策，须经人类确认后实施**（见 §1「美术与光照例外」）；
  - 剔除：合理使用 `VisibleOnScreenNotifier` / 手动 `hide()`，避免不可见节点仍参与渲染；
  - 粒子：优先 `GPUParticles`（Android 导出注意兼容性），数量受控。
- **多线程**：耗时计算用 `WorkerThreadPool.add_task()`（Godot 4.1+）或 `Thread`；注意线程安全（用 `Mutex` / `Semaphore`，避免跨线程操作场景树——场景树只能主线程访问）。
- **物理**：`_physics_process` 保持轻量；碰撞形状尽量用基础几何体（Box / Sphere / Capsule），避免高面数 `ConcavePolygonShape` 对撞。
- **内存**：纹理用导入压缩（VRAM 压缩 / 尺寸限制）；音频用 Ogg 流式（`stream` 模式）而非全量载入；场景切换用 `change_scene_to_file` 时确保旧场景引用释放（断开信号、停止线程）。**注：材质与音频内容本身属人类主导（见 §1「美术与光照例外」），此处仅限技术加载策略。**
- **性能剖析**：优化前先量化——用 Godot Profiler、`Performance` 单例、`Engine.get_frames_per_second()`；禁止无数据支撑的"优化"。
- **Android 专项**：渲染用 Mobile 渲染器或 Forward+（低端设备用 Mobile）；纹理压缩选 ASTC（Android 8+ 原生支持）/ ETC2；APK 包体控制（首装 ≤ 100MB 为佳，资源尽量按需下载或分包）；后台切换时暂停 / 释放资源。

## 7. 测试与 CI

- **现状**：没有 CI。手动验证：`godot --headless --path .` 运行测试脚本（GUT / gdUnit4，位于 `addons/` 与 `tests/`）。
- **Agent 义务**：涉及逻辑 / 场景 / 资源的功能改动，Agent 必须在 PR 描述写明验证方式；能跑测试就跑一遍，不能跑要说明原因。Agent 提交前必须实际运行验证（`godot --headless` 至少能加载项目无报错），不能只做静态分析就声称完成。
- **CI 规划（待建设）**：未来接入 GitHub Actions，覆盖 Windows 与 Android 的 headless 加载与测试矩阵（Windows 用官方 runner；Android 可在 Linux runner 上做 headless 逻辑测试 + 导出构建验证）。**CI 里不能放真实密钥**——自动化测试只能覆盖「无密钥也能验证」的部分（项目加载、场景实例化、纯逻辑单测）。
- 新增测试脚本命名沿用 `test_*.gd` 风格，方便 CI 统一发现。

## 8. AI Agent 提交前自检清单

以下清单由 AI Agent 在提交（commit / PR）前逐项自查：

- [ ] `git status` 中没有密钥 / token / 密码等敏感文件
- [ ] 无本机路径、个人代理、个人账号信息残留
- [ ] `godot --headless --path .` 可正常加载项目无报错
- [ ] 至少跑一遍相关测试脚本（`tests/` 下的 GUT / gdUnit4 测试），或说明为什么不适用
- [ ] 新增 / 修改的功能已在 README 或 docs 文档中登记
- [ ] 模块化检查：功能已按模块划分、模块间仅经 glue 接线（无直接引用）
- [ ] 工程独立性检查：Agent 专属文件均在 `agent/` 下；工程未引用 `res://agent/` 内容；删除 `agent/` 后工程仍可加载运行
- [ ] 创建项目前已询问并确认存放路径（若涉及创建 / 初始化 Godot 项目）
- [ ] godot-mcp 检测：涉及编辑器交互前已检测是否安装（`addons/` 下插件 / 配置）；未安装已告知使用者并请求安装；若经同意由 Agent 介入安装，已完成安装并复检，未阻塞开发（详见 AGENTS.md §1.4）
- [ ] 复用检查：实现前已检索库内可复用资源（已有模块 / 共享类 / 资源清单），未重复造轮子
- [ ] 无美术资源残留：commit 中不含贴图 / 音频 / 模型 / 字体（含 `assets/`、`modules/*/assets/` 及常见资源格式）
- [ ] 场景 / 资源变更已在 PR 描述中说明
- [ ] 光照 / 美术 / 材质 / 音频相关改动（如有）已获人类确认
- [ ] 未代为执行人工操作边界内的动作（Godot 安装、项目骨架创建）
- [ ] 热点路径已检查 §6 性能红线（无每帧分配 / IO / 网络 / 资源加载）
- [ ] 多平台考虑：Windows / Android 平台差异的代码已用特性检测门控（`OS.has_feature()`）
- [ ] 提交信息符合 Conventional Commits 格式
